use Test::More tests => 1;

#use Protocol::OpenID::Signature;

#my $s = Protocol::OpenID::Signature->new;

#ok(not defined $s->calculate);

#$s->signed('foo');
#$s->params({'openid.foo' => 'hello'});
##is($s->calculate, '');
ok(1);
